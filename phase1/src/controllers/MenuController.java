package controllers;

import entities.User;

/** A menu controller of which handles user input from the menu interface */
public class MenuController {

    /**
     * Verify and change interface based on menu choice
     *
     * @param menuChoice the menu choice made by the user
     */
    public static void handleMenuChoice(String menuChoice, User user) {
//        if (user.getMenuOptions().contains(menuChoice)) {
//
//        }
        // if user options contains this menu choice then:
        // direct to corresponding interface, ie. messages, contacts etc.
    }
}
